"use client";
import { useEffect, useState, type FormEvent, type ReactNode } from "react";
import { Check, KeyRound, Pencil, Plus, Search, Shield, ShieldCheck, Trash2, UserCheck, X } from "lucide-react";
import { PanelShell } from "@/components/layout/PanelShell";
type Perfil={id:string;name:string;description:string;permissions:string[];system?:boolean};const defaults:Perfil[]=[{id:"admin",name:"Administrador",description:"Acesso completo à configuração e à operação da revenda.",permissions:["Visualizar painel","Gerenciar usuários","Gerenciar perfis e permissões","Cadastrar veículos","Registrar custos e despesas","Registrar vendas"],system:true},{id:"operador",name:"Operador",description:"Base para vendedores e assistentes.",permissions:["Visualizar painel","Cadastrar veículos"]}];const available=["Visualizar painel","Gerenciar usuários","Gerenciar perfis e permissões","Cadastrar veículos","Registrar custos e despesas","Registrar vendas"];
export default function Perfis(){const[roles,setRoles]=useState<Perfil[]>(defaults),[search,setSearch]=useState(""),[modal,setModal]=useState<"new"|Perfil|null>(null),[form,setForm]=useState({name:"",description:"",permissions:[] as string[]});useEffect(()=>{const saved=localStorage.getItem("revenda-pro-roles");if(saved)setRoles(JSON.parse(saved))},[]);function persist(next:Perfil[]){setRoles(next);localStorage.setItem("revenda-pro-roles",JSON.stringify(next))}function edit(p:Perfil|"new"){setForm(p==="new"?{name:"",description:"",permissions:[]}: {name:p.name,description:p.description,permissions:p.permissions});setModal(p)}function save(e:React.FormEvent){e.preventDefault();if(!form.name||!form.permissions.length)return;if(modal==="new")persist([...roles,{id:crypto.randomUUID(),...form}]);else if(modal)persist(roles.map(x=>x.id===modal.id?{...x,...form}:x));setModal(null)}function toggle(p:string){setForm(f=>({...f,permissions:f.permissions.includes(p)?f.permissions.filter(x=>x!==p):[...f.permissions,p]}))}const view=roles.filter(x=>`${x.name} ${x.description}`.toLowerCase().includes(search.toLowerCase()));return <PanelShell><div className="dash-anim"><div className="mb-5 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-1 text-xs font-bold uppercase tracking-[.18em] text-[var(--primary)]">Acessos</p><h1 className="text-2xl font-bold">Perfis e permissões</h1><p className="mt-1 text-sm text-[var(--text-secondary)]">Defina o que cada perfil pode acessar em cada tela do sistema.</p></div><button onClick={()=>edit("new")} className="flex items-center gap-2 rounded-md bg-[var(--primary)] px-4 py-2 text-sm font-semibold text-white"><Plus size={16}/>Novo perfil</button></div><div className="mb-5 rounded-xl border border-[color-mix(in_srgb,var(--primary)_30%,var(--border))] bg-[color-mix(in_srgb,var(--primary)_7%,var(--surface))] p-4 text-sm text-[var(--text-secondary)]"><p className="font-semibold text-[var(--text-primary)]">Perfis personalizados por revenda</p><p className="mt-1">Crie perfis para financeiro, vendedor, oficina ou qualquer pessoa da sua operação. O menu e o acesso direto às páginas seguem estas permissões.</p></div><div className="mb-5 flex flex-wrap items-end gap-3 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-4"><label className="relative flex min-w-64 flex-1 flex-col gap-1.5 text-xs font-medium text-[var(--text-secondary)]">Buscar perfil<span className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--text-muted)]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Nome, descrição ou permissão" className="w-full rounded-md border border-[var(--border)] bg-[var(--canvas)] py-2 pl-9 pr-3 text-sm text-[var(--text-primary)]"/></span></label></div><div className="mb-5 grid gap-4 sm:grid-cols-3"><Summary title="Perfis" value={roles.length} icon={Shield}/><Summary title="Customizáveis" value={roles.filter(x=>!x.system).length} icon={KeyRound}/><Summary title="Usuários vinculados" value={1} icon={UserCheck}/></div><div className="overflow-hidden rounded-xl border border-[var(--border)] bg-[var(--surface)] shadow-[var(--shadow)]"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-[var(--border)] bg-[var(--surface-2)] text-xs uppercase tracking-wider text-[var(--text-muted)]"><tr><th className="px-5 py-3">Perfil</th><th className="px-5 py-3">Permissões por tela</th><th className="px-5 py-3">Tipo</th><th className="px-5 py-3 text-right">Ações</th></tr></thead><tbody className="divide-y divide-[var(--border)]">{view.map(r=><tr key={r.id} className="hover:bg-[var(--surface-2)]"><td className="px-5 py-4"><p className="font-semibold">{r.name}</p><p className="mt-1 max-w-sm text-xs text-[var(--text-muted)]">{r.description}</p></td><td className="px-5 py-4"><div className="flex max-w-md flex-wrap gap-1.5">{r.permissions.map(p=><span key={p} className="rounded-full bg-[color-mix(in_srgb,var(--primary)_9%,transparent)] px-2 py-1 text-[11px] font-semibold text-[var(--primary)]">{p}</span>)}</div></td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${r.system?"bg-[color-mix(in_srgb,var(--warning)_12%,transparent)] text-[var(--warning)]":"bg-[var(--surface-2)] text-[var(--text-secondary)]"}`}>{r.system?"Sistema":"Customizável"}</span></td><td className="px-5 py-4"><div className="flex justify-end"><button onClick={()=>edit(r)} aria-label={`Editar ${r.name}`} className="grid h-9 w-9 place-items-center rounded-md text-[var(--primary)] hover:bg-[var(--surface-2)]"><Pencil size={16}/></button><button disabled={r.system} onClick={()=>persist(roles.filter(x=>x.id!==r.id))} aria-label={`Excluir ${r.name}`} className="grid h-9 w-9 place-items-center rounded-md text-[var(--critical)] hover:bg-[var(--surface-2)] disabled:cursor-not-allowed disabled:opacity-30"><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div></div>{modal&&<Dialog title={modal==="new"?"Novo perfil":`Editar perfil — ${modal.name}`} close={()=>setModal(null)}><form onSubmit={save} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><label className="text-xs font-medium text-[var(--text-secondary)]">Nome do perfil<input disabled={modal!=="new"&&modal.system} value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required className="mt-1.5 w-full rounded-md border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-sm disabled:opacity-60"/></label><label className="text-xs font-medium text-[var(--text-secondary)] sm:col-span-2">Descrição<textarea value={form.description} onChange={e=>setForm({...form,description:e.target.value})} rows={2} className="mt-1.5 w-full resize-none rounded-md border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-sm"/></label></div><section><h3 className="mb-2 text-sm font-semibold">Permissões por tela</h3><p className="mb-3 text-xs text-[var(--text-muted)]">Escolha as telas que este perfil pode acessar.</p><div className="grid gap-2 sm:grid-cols-2">{available.map(p=>{const yes=form.permissions.includes(p);return <label key={p} className={`flex cursor-pointer items-start gap-3 rounded-lg border p-3 ${yes?"border-[var(--primary)] bg-[color-mix(in_srgb,var(--primary)_7%,var(--surface))]":"border-[var(--border)] bg-[var(--surface)]"}`}><input type="checkbox" checked={yes} onChange={()=>toggle(p)} className="mt-1"/><span><span className="block text-sm font-semibold">{p}</span><span className="mt-.5 block text-[11px] text-[var(--text-muted)]">Permissão de acesso</span></span></label>})}</div></section><div className="flex justify-end gap-3"><button type="button" onClick={()=>setModal(null)} className="rounded-md border border-[var(--border)] px-4 py-2 text-sm font-semibold">Cancelar</button><button disabled={!form.permissions.length} className="flex items-center gap-2 rounded-md bg-[var(--primary)] px-4 py-2 text-sm font-semibold text-white disabled:opacity-60"><ShieldCheck size={16}/>Salvar perfil</button></div></form></Dialog>}</div></PanelShell>}
function Summary({title,value,icon:Icon}:{title:string;value:number;icon:typeof Shield}){return <article className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-5 shadow-[var(--shadow)]"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--text-muted)]">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><Icon size={28} className="text-[var(--primary)] opacity-60"/></div></article>};function Dialog({title,children,close}:{title:string;children:ReactNode;close:()=>void}){return <div className="fixed inset-0 z-[60] grid place-items-center bg-[#07152c]/60 p-4"><div className="max-h-[90vh] w-full max-w-3xl overflow-auto rounded-xl border border-[var(--border)] bg-[var(--canvas)] shadow-[var(--shadow-lg)]"><div className="flex items-center justify-between border-b border-[var(--border)] px-5 py-4"><h2 className="text-lg font-bold">{title}</h2><button onClick={close} className="grid h-8 w-8 place-items-center rounded-md hover:bg-[var(--surface-2)]"><X size={18}/></button></div><div className="p-5">{children}</div></div></div>}
