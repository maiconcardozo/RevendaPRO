"use client";
import { useEffect, useMemo, useState, type FormEvent, type ReactNode } from "react";
import { Pencil, Plus, RefreshCw, Search, ShieldCheck, Trash2, UserCheck, Users, X } from "lucide-react";
import { PanelShell } from "@/components/layout/PanelShell";
type Usuario={id:string;name:string;email:string;role:string;active:boolean}; const seed:Usuario[]=[{id:"admin",name:"Administrador",email:"admin@revendapro.local",role:"Administrador",active:true}];
export default function Usuarios(){const[users,setUsers]=useState< Usuario[]>(seed),[search,setSearch]=useState(""),[modal,setModal]=useState<"new"|Usuario|null>(null),[remove,setRemove]=useState<Usuario|null>(null),[form,setForm]=useState({name:"",email:"",role:"Operador",active:true});useEffect(()=>{const saved=localStorage.getItem("revenda-pro-users");if(saved)setUsers(JSON.parse(saved))},[]);function persist(next:Usuario[]){setUsers(next);localStorage.setItem("revenda-pro-users",JSON.stringify(next))}function openNew(){setForm({name:"",email:"",role:"Operador",active:true});setModal("new")}function openEdit(u:Usuario){setForm({name:u.name,email:u.email,role:u.role,active:u.active});setModal(u)}function save(e:FormEvent){e.preventDefault();if(!form.name.trim()||!form.email.trim())return;if(modal==="new")persist([...users,{id:crypto.randomUUID(),...form}]);else if(modal)persist(users.map(u=>u.id===modal.id?{...u,...form}:u));setModal(null)}const filtered=useMemo(()=>users.filter(u=>`${u.name} ${u.email} ${u.role}`.toLocaleLowerCase("pt-BR").includes(search.toLocaleLowerCase("pt-BR"))),[users,search]);return <PanelShell><div className="dash-anim"><div className="mb-5 flex flex-wrap items-end justify-between gap-4"><div><p className="mb-1 text-xs font-bold uppercase tracking-[.18em] text-[var(--primary)]">Acessos</p><h1 className="text-2xl font-bold">Usuários</h1><p className="mt-1 text-sm text-[var(--text-secondary)]">Gerencie usuários, perfis e permissões da revenda.</p></div><div className="flex flex-wrap gap-3"><a href="/perfis" className="flex items-center gap-2 rounded-md border border-[var(--primary)] px-4 py-2 text-sm font-semibold text-[var(--primary)]"><ShieldCheck size={16}/>Perfis e permissões</a><button onClick={openNew} className="flex items-center gap-2 rounded-md bg-[var(--primary)] px-4 py-2 text-sm font-semibold text-white"><Plus size={16}/>Novo usuário</button></div></div><div className="mb-5 flex flex-wrap items-end gap-3 rounded-xl border border-[var(--border)] bg-[var(--surface)] p-4"><label className="relative flex min-w-64 flex-1 flex-col gap-1.5 text-xs font-medium text-[var(--text-secondary)]">Buscar<span className="relative"><Search size={16} className="absolute left-3 top-1/2 -translate-y-1/2 text-[var(--text-muted)]"/><input value={search} onChange={e=>setSearch(e.target.value)} placeholder="Nome, e-mail ou perfil" className="w-full rounded-md border border-[var(--border)] bg-[var(--canvas)] py-2 pl-9 pr-3 text-sm text-[var(--text-primary)]"/></span></label><button className="flex h-10 items-center gap-2 rounded-md border border-[var(--border)] px-4 text-sm font-semibold" onClick={()=>setSearch("")}><RefreshCw size={15}/>Atualizar</button></div><div className="mb-5 grid gap-4 sm:grid-cols-3"><Summary title="Usuários" value={users.length} icon={Users}/><Summary title="Ativos" value={users.filter(x=>x.active).length} icon={UserCheck}/><Summary title="Perfis disponíveis" value={2} icon={ShieldCheck}/></div><div className="overflow-hidden rounded-xl border border-[var(--border)] bg-[var(--surface)] shadow-[var(--shadow)]"><div className="overflow-x-auto"><table className="w-full min-w-[760px] text-left text-sm"><thead className="border-b border-[var(--border)] bg-[var(--surface-2)] text-xs uppercase tracking-wider text-[var(--text-muted)]"><tr><th className="px-5 py-3">Usuário</th><th className="px-5 py-3">Perfis</th><th className="px-5 py-3">Permissões</th><th className="px-5 py-3">Status</th><th className="px-5 py-3 text-right">Ações</th></tr></thead><tbody className="divide-y divide-[var(--border)]">{filtered.map(u=><tr key={u.id} className="hover:bg-[var(--surface-2)]"><td className="px-5 py-4"><p className="font-semibold">{u.name}</p><p className="text-xs text-[var(--text-muted)]">{u.email}</p></td><td className="px-5 py-4"><span className="rounded-full bg-[color-mix(in_srgb,var(--primary)_10%,transparent)] px-2 py-1 text-[11px] font-semibold text-[var(--primary)]">{u.role}</span></td><td className="px-5 py-4 text-[var(--text-secondary)]">{u.role==="Administrador"?"6":"2"} acessos</td><td className="px-5 py-4"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${u.active?"bg-[color-mix(in_srgb,var(--success)_12%,transparent)] text-[var(--success)]":"bg-[var(--surface-2)] text-[var(--text-muted)]"}`}>{u.active?"Ativo":"Inativo"}</span></td><td className="px-5 py-4"><div className="flex justify-end"><button onClick={()=>openEdit(u)} aria-label={`Editar ${u.name}`} className="grid h-9 w-9 place-items-center rounded-md text-[var(--primary)] hover:bg-[var(--surface-2)]"><Pencil size={16}/></button><button onClick={()=>setRemove(u)} disabled={u.id==="admin"} aria-label={`Excluir ${u.name}`} className="grid h-9 w-9 place-items-center rounded-md text-[var(--critical)] hover:bg-[var(--surface-2)] disabled:cursor-not-allowed disabled:opacity-30"><Trash2 size={16}/></button></div></td></tr>)}</tbody></table></div>{!filtered.length&&<div className="p-12 text-center text-sm text-[var(--text-muted)]">Nenhum usuário encontrado.</div>}</div>{modal&&<Dialog title={modal==="new"?"Novo usuário":"Editar usuário"} close={()=>setModal(null)}><form onSubmit={save} className="space-y-5"><div className="grid gap-4 sm:grid-cols-2"><Field label="Nome completo" value={form.name} onChange={name=>setForm({...form,name})}/><Field label="E-mail" type="email" value={form.email} onChange={email=>setForm({...form,email})}/><label className="text-xs font-medium text-[var(--text-secondary)]">Perfil<select value={form.role} onChange={e=>setForm({...form,role:e.target.value})} className="mt-1.5 w-full rounded-md border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-sm"><option>Administrador</option><option>Operador</option></select></label><label className="flex items-center gap-3 self-end rounded-md border border-[var(--border)] px-3 py-2.5 text-sm"><input type="checkbox" checked={form.active} onChange={e=>setForm({...form,active:e.target.checked})}/>Usuário ativo</label></div><div className="flex justify-end gap-3"><button type="button" onClick={()=>setModal(null)} className="rounded-md border border-[var(--border)] px-4 py-2 text-sm font-semibold">Cancelar</button><button className="rounded-md bg-[var(--primary)] px-4 py-2 text-sm font-semibold text-white">Salvar usuário</button></div></form></Dialog>}{remove&&<Dialog title="Excluir usuário" close={()=>setRemove(null)}><p className="text-sm text-[var(--text-secondary)]">Deseja excluir <strong className="text-[var(--text-primary)]">{remove.name}</strong>? A conta perderá o acesso.</p><div className="mt-6 flex justify-end gap-3"><button onClick={()=>setRemove(null)} className="rounded-md border border-[var(--border)] px-4 py-2 text-sm font-semibold">Cancelar</button><button onClick={()=>{persist(users.filter(u=>u.id!==remove.id));setRemove(null)}} className="rounded-md bg-[var(--critical)] px-4 py-2 text-sm font-semibold text-white">Excluir</button></div></Dialog>}</div></PanelShell>}
function Summary({title,value,icon:Icon}:{title:string;value:number;icon:typeof Users}){return <article className="rounded-xl border border-[var(--border)] bg-[var(--surface)] p-5 shadow-[var(--shadow)]"><div className="flex items-center justify-between"><div><p className="text-xs font-bold uppercase tracking-wider text-[var(--text-muted)]">{title}</p><p className="mt-2 text-3xl font-bold">{value}</p></div><Icon size={28} className="text-[var(--primary)] opacity-60"/></div></article>};function Field({label,value,onChange,type="text"}:{label:string;value:string;onChange:(v:string)=>void;type?:string}){return <label className="text-xs font-medium text-[var(--text-secondary)]">{label}<input type={type} value={value} onChange={e=>onChange(e.target.value)} required className="mt-1.5 w-full rounded-md border border-[var(--border)] bg-[var(--surface)] px-3 py-2 text-sm"/></label>};function Dialog({title,children,close}:{title:string;children:ReactNode;close:()=>void}){return <div className="fixed inset-0 z-[60] grid place-items-center bg-[#07152c]/60 p-4"><div className="w-full max-w-2xl rounded-xl border border-[var(--border)] bg-[var(--canvas)] shadow-[var(--shadow-lg)]"><div className="flex items-center justify-between border-b border-[var(--border)] px-5 py-4"><h2 className="text-lg font-bold">{title}</h2><button onClick={close} aria-label="Fechar" className="grid h-8 w-8 place-items-center rounded-md hover:bg-[var(--surface-2)]"><X size={18}/></button></div><div className="p-5">{children}</div></div></div>}
